/*
 * number
 * --int
 * --double(fraction part)
 * string(single character also string)--String(representation)
 * Boolean(bool)--default "null"
 * list
 * maps
 * runes(for expressing unicode expressions in strings)
 * symbols
*/

void main(){
  // number: int
  int age=10;
  
  // numbers: double
  double price=10.50;
  
  //strings
  
  String name="sai baba";
  
  //boolean
  
  bool alive=true;
  bool dead=false;
  bool ne;
  
  
  // or we can directly use the var keyword
  // it will decide own
  
  var aage=10;
  print(age);
  
}
